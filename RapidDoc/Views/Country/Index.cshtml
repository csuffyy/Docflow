@{
    ViewBag.Title = CustomRes.Custom.Country;
}

<h2>@CustomRes.Custom.Country</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
    <script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetCountryList", "Country")")

    var detailsUrl = '@Url.Action("Edit", "Country")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
    </script>
}
