@model IEnumerable<RapidDoc.Models.ViewModels.IncomingDublicateView>

@if(Model.Count() > 0)
{
    <h5><strong>Дубликаты:</strong></h5>
    foreach (var item in Model)
    {
        <p class="text-danger">(@item.CompanyName) @item.CreatedBy, @item.RegistrationDate</p>
    }
}
