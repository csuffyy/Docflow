@{
    ViewBag.Title = CustomRes.Custom.QuestionRequest;
}

<h2>@CustomRes.Custom.QuestionRequest</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
    <script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetQuestionRequestList", "QuestionRequest")")

    var detailsUrl = '@Url.Action("Edit", "QuestionRequest")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
    </script>
}
