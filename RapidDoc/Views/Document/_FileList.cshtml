@model IEnumerable<RapidDoc.Models.DomainModels.FileTable>

@if (Model.Count() > 0)
{
    foreach (var item in Model)
    {
        <p>
            <a href=@Url.Action(@"DownloadFile/" + item.Id.ToString(), "Document") class="">@item.FileName</a>
        </p>
    }
}

