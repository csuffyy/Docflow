@{
    ViewBag.Title = CustomRes.Custom.IpList;
}

<h2>@CustomRes.Custom.IpList</h2>

@{Html.RenderPartial("_NewEntityBtn");}

<div>
    @Html.Action("Grid")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/gridmvc")
}
@section jslaunch {
    <script type="text/javascript">
$(function () {
    grid_init("@Url.Action("GetIpListList", "IpList")")

    var detailsUrl = '@Url.Action("Edit", "IpList")';
    pageGrids.ordersGrid.onRowSelect(function (e) {
    window.location.href = detailsUrl  + '/' + e.row.Id;
    });
});
    </script>
}
